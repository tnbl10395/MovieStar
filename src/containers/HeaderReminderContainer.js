import { connect } from 'react-redux';
import { HeaderReminder } from '../templates/Header';

const mapStateToProps = (state) => ({
});

const mapDispatchToProps = (dispatch) => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(HeaderReminder);